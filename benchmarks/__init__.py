"""ECS benchmark packages (package marker so enterprise modules import cleanly).

Adding this marker does not change the existing script-style runners, which are
still invoked as ``python benchmarks/ai_workload/benchmark_runner.py``.
"""
